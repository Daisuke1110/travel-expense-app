• 本番デプロイ前は、最低でもこの順で進めるのが安全です。

1. 受け入れテスト（dev実環境）

- 主要フローをE2Eで確認: 一覧取得、トリップ作成/更新/削除、メンバー追加/削除、支出追加/編集/削除。
- APIエラー時の表示も確認（ネットワーク断・4xx/5xx）。

2. MVP仕様ギャップ修正

- 金額入力を整数のみ許可に統一（設計は整数必須）。frontend/src/pages/AddExpensePage.tsx:107
- PWA要件（manifest と service worker）を実装。現状未配置。

3. 認証まわりの整理（最重要）

- 本番では X-Debug-User-Id 前提をやめる計画を確定。
- 現状はフロント送信＋バックエンド必須なので、そのまま本番は避ける。frontend/src/api/client.ts:7 backend/app/routes/trips.py:126

4. 本番Terraform値・設定の最終確認

- infra/terraform/envs/prod.tfvars のドメイン、CORS、証明書ARN、PITR有効を再確認。
- lambda.zip が最新ソースから作られていることを確認。

5. 運用設定

- CloudWatch Logsの保持期間を設定（dev/prodで分ける）。
- 障害時の確認手順（どのログを見るか）を1枚にまとめる。

6. データ準備

- Users テーブルに本番利用者を事前登録（owner_name表示のため）。

7. リリース手順の固定化

- plan 差分確認 → apply → フロント配信 → 動作確認（トップページとAPI）までをチェックリスト化。

必要なら次で、このまま使える「本番リリース前チェックリスト（Yes/No形式）」を作ります。

8. ユーザー追加機能の選定

| No  | 項目                                                               | Yes/No |
| --- | ------------------------------------------------------------------ | ------ |
| 1   | prod.tfvars の domain/cert/CORS/PITR 設定をレビュー済み            |        |
| 2   | Terraform plan 差分を確認し、意図しない変更がない                  |        |
| 3   | lambda.zip が最新ソースから再作成されている                        |        |
| 4   | フロント dist が最新コミットからビルドされている                   |        |
| 5   | devで主要フローE2E（一覧/作成/更新/削除/メンバー/支出CRUD）が成功  |        |
| 6   | APIエラー時の画面表示（4xx/5xx/オフライン）を確認済み              |        |
| 7   | 金額入力が整数のみ（小数NG）になっている                           |        |
| 8   | PWA要件（manifest / service worker）が実装・動作確認済み           |        |
| 9   | X-Debug-User-Id を本番で使わない構成（または本番遮断）になっている |        |
| 10  | 認証・認可（owner/member）の権限テストがdevで通過                  |        |
| 11  | Usersデータ（本番利用者）を事前投入済み                            |        |
| 12  | CloudWatch Logs の確認手順をチームで共有済み                       |        |
| 13  | ログ保持期間（dev/prod）を設定済み                                 |        |
| 14  | 障害時ロールバック手順（前バージョン復旧）を文書化済み             |        |
| 15  | リリース手順書（実行順・担当・確認ポイント）を確定済み             |        |
| 16  | 本番デプロイ後の疎通確認項目（トップ/API/主要操作）を定義済み      |        |
| 17  | 本番デプロイ後の初期監視（30-60分）担当者を決定済み                |        |
| 18  | リリースGo/No-Go判定者を決定済み                                   |        |

## 判定ルール

- 必須Noが1つでもあればリリース中止
- 推奨: Yes 18/18 で本番実施
