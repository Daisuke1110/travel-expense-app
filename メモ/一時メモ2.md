メモ/一時メモ.md は確認しました。Step 6 の意図は明確で、API が返す name 系フィールドをフロントの表示に反映するだけです。大
きなロジック追加は不要で、主に型定義と表示箇所の差し替えになります。

実装対象はこの4か所です。

1. API 型を更新する
   frontend/src/api/trips.ts の TripMemberResponse に name?: string | null を追加します。
   frontend/src/api/expenses.ts の ExpenseItem に paid_by_name?: string | null と、Step 5 で返しているなら user_name?:
   string | null を追加します。
2. Trip メンバー一覧を name ?? user_id に変える
   frontend/src/pages/TripDetailPage.tsx の members-list\_\_user は今 member.user_id を直接表示しています。ここを
   member.name ?? member.user_id に変えます。
   同じファイルの Paid totals でも、いまは item.userId を表示しているので、memberState.data から対応する member を引いて
   name ?? user_id で出す形にします。ここは Map<user_id, displayName> を useMemo で作るのが一番素直です。
3. Expense の Paid by 表示を paid_by_name ?? paid_by_user_id に変える
   frontend/src/components/ExpenseItem.tsx の
   Paid by {item.paid_by_user_id} を Paid by {item.paid_by_name ?? item.paid_by_user_id} に変えます。
   加えて編集フォームの select の候補も member.user_id ではなく member.name ?? member.user_id を表示に使います。value は引
   き続き member.user_id のままでよいです。
4. owner 表示を必要なら fallback 付きで調整する
   frontend/src/pages/TripDetailPage.tsx と frontend/src/components/TripCard.tsx はすでに owner_name を使っています。ここは
   owner_name ?? owner_id ?? "owner" のようにしておくと fallback が一貫します。

実装イメージはこうです。

const memberNameById = useMemo(() => {
return new Map(
memberState.data.map((member) => [
member.user_id,
member.name ?? member.user_id,
]),
{member.name ?? member.user_id}

{item.paid_by_name ?? item.paid_by_user_id}

要するに Step 6 は、

- API 型に name 系を追加
- 表示文字列を name ?? user_id に置換
- select のラベルも名前優先に変更

これで完了です。必要なら次に、そのまま適用できる差分として実装します。
