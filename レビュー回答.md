設計書/frontend.md:
レビュー内容：オフライン時のエラー表示方針は requirements に合わせて文言・動作を明記すると実装がぶれにくいです。
レビュー回答：

- API がオフラインエラー（fetch 失敗）を返した場合：
- "現在オフラインです。接続が回復したら自動的に同期します。" を表示
- 入力はローカルキュー（IndexedDB）に保存し、オンライン復帰時に同期する（将来対応）。

設計書/architecture.md：
レビュー内容：Lambda を VPC 外で動かす方針は明記されているが、CORS/HTTPS 終端の責務分けをもう少し具体化するとデプロイ時の設定漏れを防げます。
レビュー回答：

### HTTPS 終端

- API Gateway（Regional）で HTTPS 終端
- CloudFront はフロント配信用のみ使用する

### CORS 設定

- AllowedOrigins:
  - https://travel-dev.daisuke-selfstudy.com
  - https://travel.daisuke-selfstudy.com
- AllowedMethods: GET, POST, PATCH, DELETE

設計書/infra.md
レビュー内容: CloudFront↔S3 間の OAC/OAI は触れているが、API Gateway の CORS 設定やカスタムドメイン/証明書の具体値（dev/prod）が未定。
Terraform モジュール構成案は良いが、秘匿情報の扱い（tfvars への認証情報を置かない等）も一言あると安心。
レビュー回答：

### API Gateway CORS 設定

allow_origins = [
"https://travel-dev.daisuke-selfstudy.com",
"https://travel.daisuke-selfstudy.com"
]

### ACM 証明書（us-east-1）

- dev: arn:aws:acm:us-east-1:xxx:certificate/aaa...
- prod: arn:aws:acm:us-east-1:xxx:certificate/bbb...

### Terraform 秘匿情報取り扱い

- tfvars に認証情報は置かない
- AWS 認証は SSO または GitHub Actions OIDC を使用
- .gitignore に _.auto.tfvars, _.tfstate を追加
